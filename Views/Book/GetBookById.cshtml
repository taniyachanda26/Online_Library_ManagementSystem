@model Online_Library_Management_System.Models.Book
@{
    ViewData["Title"] = "GetBookById";
}
<div class="container">
    <h3 class="display-4">Searched Book</h3>
    <div class="row">
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <img src="@Model.ImagePath" class="bd-placeholder-img card-img-top" width="100%" hight="225" alt="book_image"></img>svg class="bd-placeholder-img card-img-top" width="100%" hight="225"></svg>
                <div class="card-body">
                    <h3 class="card-title">@Model.Title</h3>
                    <p class="card-text">This Section for description</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a href="#" class="btn btn-sm btn-outline-secondary">View Details</a>

                        </div>
                        <small class="text-muted">@Model.Author</small>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
